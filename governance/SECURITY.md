# Security

Report vulnerabilities privately to the repository maintainers. Never include gateway tokens, WeChat credentials, state directories, signing keys, or QR login data in issues or artifacts. Registry validation is structural and does not establish trust. M0/M1 candidates are explicitly unsigned; verify the SHA-256 inventory before use.
